# 里程碑计划

> **不排日期**，按「依赖顺序 + 价值密度」排。**每个里程碑都要能独立演示**——中途停下也有拿得出手的东西。
> 每完成一个：更新 README 的演示说明 + 记录踩坑。

---

## M0 · 工程骨架

**目标**：先把"工程"立起来，避免后面散成一堆文件。

- 仓库结构：`app/` `modules/` `common/`（见 ARCHITECTURE）
- CMake 构建（支持 Debug/Release）
- `common/`：日志（分级、带时间戳）、配置解析（命令行/配置文件）、错误处理约定
- 单元测试框架接入（Catch2 或 GoogleTest）

**交付物**：能编译出空壳可执行；日志能打；一个示例单测跑通。
**验收**：`cmake --build` 一次成功；`./lowlat_sender --help` 有输出；`ctest` 通过。

---

## M1 · 采集 + 编码

**目标**：把画面变成 H.264 码流。

- **Null 模拟源**：生成可辨识的测试画面（色块 + 帧号），无摄像头也能跑 ← **先做这个**
- **V4L2 采集**（v4l2loopback 虚拟设备验证，见 [NOTES 第 17 条](docs/NOTES.md)）
- 编码：libavcodec/x264，低延迟参数（`zerolatency` / `-bf 0` / 短 GOP）
- 采集与编码之间用有界队列解耦

**交付物**：`lowlat_sender --source=null|v4l2 --dump=out.h264`
**验收**：dump 出的 `.h264` 能被 ffplay 播放；帧号可辨认；能切换两种源。

---

## M2 · UDP 传输（点对点）

**目标**：码流能过网络送到对端并完整还原。

- 自研协议包头（见 PROTOCOL）：全局 `seq` + `frame_id/frag_index/frag_count`
- 发送端：`UdpSocket`（RAII）+ `Packetizer` 分片，编码与发送之间再加一级有界队列
- 接收端：`FrameAssembler` 按 `frame_id` 重组、按 `seq` 统计丢包/乱序
- 统计：收发包数、还原帧数、丢包数、畸形包数、放弃的残帧数

**交付物**：`lowlat_sender --target=…` → `lowlat_receiver --dump=recv.h264`
**验收**：无丢包时 `recv.h264` 与发送端 dump **逐字节一致**；统计数字对得上。

> `sendmsg`/`iovec` 零拷贝**没做**：现在是把包头和载荷拼进一块连续缓冲再 `sendto`。
> 省掉的那次 memcpy 是 1200 字节 × 每秒几十次，还没有证据说它值得，
> 留到 M6 压测时按火焰图决定（见 README「已知限制」）。

---

## M3 · 接收端解码渲染（端到端出画面）🎯

**目标**：**第一次看到实时画面**——最提气的里程碑。

- 接收线程 → 解码线程 → 渲染线程（三线程管线，队列解耦）
- FFmpeg 解码 H.264 → SDL2 渲染
- 基础延迟测量：发送端打时间戳，接收端算端到端延迟

**交付物**：跑起来能看到实时画面 + 终端打印当前延迟
**验收**：画面流畅、无明显卡顿；端到端延迟有数字。

---

## M4 · 弱网对抗 ⭐核心亮点

**目标**：网络变差时画面"尽量不崩"。

- **丢包/延迟注入工具**（发送端可配 `--loss=5% --jitter=30ms`）——先有测试手段
- **NACK 重传**：接收端检 `seq` 缺口 → 请求重发；发送端维护重传缓存
- **FEC(XOR)**：每 K 个包配 1 个冗余包，丢 1 可恢复
- **自适应 jitter buffer**：按实测抖动动态调水位
- **关键帧请求(PLI)**：无法恢复时请求新 IDR
- 指标：FEC 恢复率、重传次数、卡顿次数、延迟 **p50/p95/p99**

**交付物**：`--loss=10%` 下仍能正常观看，指标面板可见恢复情况
**验收**：能给出「X% 丢包下不花屏 / 端到端 Y ms」的量化结论；对比开关 FEC/NACK 的效果差异。

---

## M5 · 分发服务端 ⭐后端亮点

**目标**：从点对点升级为**一推多拉**。

- 房间管理：房间号、推流端与拉流端配对、上下线、心跳超时清理
- **转发**：一路推流复制分发给 N 路拉流（SFU 雏形）
- per-client 发送队列 + **背压策略**（队满怎么办）
- 并发连接管理（epoll / Reactor）
- 信令：加入房间、开始/停止、关键帧请求转发

**交付物**：1 个推流端 + 3 个接收端同时观看同一路流
**验收**：多客户端稳定；某个接收端断开不影响其他；心跳超时能自动清理。

---

## M6 · 指标与压测

**目标**：把"我做得好"变成**数字**。

- **Redis**：实时指标（延迟分位、丢包率、码率、在线路数）+ TTL 心跳
- **HTTP API**：查询当前/历史窗口指标
- 简易展示：终端表格刷新 或 极简 web 页
- **压测**：逐步加路数，记录 CPU / 延迟 / 丢包拐点

**交付物**：压测报告（表格 + 曲线）
**验收**：能回答"单机能扛几路、瓶颈在哪、为什么"。

---

## M7 · 打磨与进阶（选做）

- README：架构图、演示 GIF/截图、一键跑起来的说明
- 拥塞控制 / **码率自适应**（按丢包率+RTT 调码率）← 含金量高
- RTP + FU-A 标准打包（可与 WebRTC/RTSP 对话）
- 音频链路（采集 + 编码 + A/V 同步）
- 代码质量：更多单测、静态检查、CI

---

## 演示优先级（如果中途要拿去面试）

| 停在这 | 你手里有什么 |
|---|---|
| M3 | 端到端实时视频传输（能演示） |
| M4 | **弱网下不崩 + 量化指标**（核心亮点已成立） |
| M5 | 一推多拉分发（后端亮点成立） |
| M6 | 压测数据（说服力最强） |

**建议至少做到 M4**——那是这个项目"有亮点"的门槛。
