# LowLat Stream — 弱网低延迟视频传输系统

> 项目总纲。工作名 `lowlat_stream`，可后期改名。

## 一句话定位

面向**弱网环境**的低延迟视频传输系统：自研 UDP 传输层（分片/FEC/重传/自适应抖动缓冲），配 C++ 分发服务端（一推多拉），端到端可观测。

## 场景设定

**远程协助 / 云游戏串流类场景**：一端持续把画面推出去，一到多个观看端实时看到，网络不稳定时画面要"尽量不崩"，延迟要尽量低。

> 场景的作用是**给技术决策提供动机**：为什么用 UDP 不用 TCP、为什么要 FEC 而不是只重传、缓冲开多大、丢包了保什么弃什么。

## 整体架构

```text
┌────────────┐        ┌──────────────────┐        ┌────────────┐
│  推流端     │        │    分发服务端      │        │  接收端     │
│ 采集→编码   │──UDP──▶│ 房间/配对/转发     │──UDP──▶│ 组包→解码   │
│ 分片→FEC    │◀─NACK──│ 指标采集(Redis)   │◀─NACK──│ 抖动缓冲→渲染│
└────────────┘        └──────────────────┘        └────────────┘
                              │
                         HTTP 指标查询
```

## 亮点分配

### 音视频侧
- 自研 UDP 传输：分片/组包、**FEC 前向纠错**、**NACK 重传**、**自适应 jitter buffer**、**关键帧请求**
- 低延迟编码参数（zerolatency / 无 B 帧 / 短 GOP）
- **可量化指标**：端到端延迟 **p50/p95/p99**、丢包率、FEC 恢复率、卡顿次数

### 后端侧
- **C++ 分发服务端**：房间管理、推拉端配对、**一路推多路拉转发**、per-client 发送队列 + 背压
- 并发连接管理（epoll / Reactor）
- **Redis** 实时指标 + TTL 心跳；**HTTP API** 查询
- **压测**：单机 N 路并发的 CPU / 延迟 / 丢包数据

## 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | C++17 | 主线语言；系统级控制力 |
| 采集 | V4L2（v4l2loopback 虚拟设备）+ Null 模拟源 | 两级可重复输入：Null 源绕过内核跑压测，虚拟设备保留完整 V4L2 调用链 |
| 编码 | libavcodec / x264 | 低延迟参数可控 |
| 传输 | **UDP + 自研协议** | 实时优先，不要 TCP 队头阻塞 |
| 抗丢包 | FEC(XOR) + NACK + PLI | 延迟/带宽/质量三者权衡 |
| 服务端 | epoll / Reactor | 复用已有网络编程基础 |
| 指标存储 | **Redis** | 高频写、短时效、需 TTL |
| 渲染 | SDL2 | 够用即可，不作卖点 |
| 构建 | CMake | 工程规范 |

## 明确的非目标（防范围膨胀）

- ❌ **不做完整播放器**：不做 seek、多格式、字幕、倍速。接收端"能收能解能显示"即可。
- ❌ **不用 MySQL**：实时流不落库，无持久化/关联查询场景，硬加是设计冗余。
- ❌ **不做三协议并列**（TCP/UDP/RTP 全上）：只把 UDP 一条做深。
- ❌ **不做完整 WebRTC**：不碰 ICE/STUN/TURN/DTLS。
- ❌ **不做 Web 管理后台**：指标只提供 API + 简易展示。
- ⏸ **RTP/FU-A 标准打包**：后期可选升级，非主线。

## 与同类项目的差异化

1. **只深挖 UDP 一条**，把 FEC/NACK/拥塞控制做透，而不是三协议并列铺面。
2. **服务端做成真正的分发节点**（房间 + 一推多拉），而非仅"采集编码源"。
3. **加码率自适应/拥塞控制**（按丢包率与 RTT 动态调码率）。
4. **用自有实验数据讲原理**：花屏传播到 IDR、GOP 与起播、PTS/DTS 错位、队列积压曲线——均有亲手实验记录。

## 文档索引

- [MILESTONES.md](MILESTONES.md) — 里程碑与验收
- [docs/PROTOCOL.md](docs/PROTOCOL.md) — 自研协议设计
- [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — 模块与线程模型
- [docs/CONVENTIONS.md](docs/CONVENTIONS.md) — 工程规范
- [docs/NOTES.md](docs/NOTES.md) — 踩坑记录
