# 测试记录

> 按轮次追加。每一轮只记三件事：**跑了什么**、**哪里出了问题**（现象 → 根因 → 后果）、
> **数据说了什么**。
>
> 和 [NOTES.md](NOTES.md) 分工：那边记**写代码时**踩的坑（编译器、API、语言规则），
> 这边记**测量时**踩的坑——工具本身出错、口径算错、实验没跑成。
> 两类坑的共同点是都会让人得出错误结论，区别是后者**不会报错**。

---

## 2026-09-01 · M4.5 第一轮全量

**跑了什么**：`scripts/m45_bench.sh` 全量，六组 60 格，每格 3 个种子 × 1000 帧
（33s @30fps），全程本机回环。commit `6835654`。

**收场**：跑完了，脚本返回 0，`summary.csv` 齐整。
**但六组里有两组一格都没跑**，而输出里看不出来。

---

### 问题 1 · `GROUPS` 是 bash 内建变量，整个实验矩阵一格没跑

**现象**　`--dry-run` 什么都不打印，脚本正常退出。

**根因**　`GROUPS` 是 bash 的**内建特殊变量**，存的是当前用户所属的 group id 列表。
脚本开头 `RUN_GROUPS=()`（当时叫 `GROUPS=()`）没能把它清空成预期的样子，
于是 `${#GROUPS[@]} -eq 0` 判定为假，默认值那一行没执行，
`wants g0` 拿 `1000 24 27 30 ...` 这些 gid 去和 `g0` 比，全部不等。

**后果**　所有 `if wants gN` 都是假，**六个组一个都没进**，脚本走到最后正常退出。
在冒烟阶段发现，因为输出是空的——**要是当时随手加了一个 `echo "开始"`，
反而会以为它跑了**。

**做法**　改名 `RUN_GROUPS`。教训是：给数组变量起名前先 `echo ${VAR[@]}` 看一眼有没有人占了。

---

### 问题 2 · 注入率的分母漏了 FEC 包和重传包

**现象**　同样 `--loss=10`，开了 FEC 的格子注入率是 **14.0%**，不开是 **10.4%**。
表面结论：**开 FEC 之后丢得更多**。

**根因**　分母用了 `tx_packets_sent`，而它**只数数据包**——
FEC 包走 `fec_packets`，重传包走 `retransmitted`，三个计数器互不重叠
（见 `SenderPipeline::sendLoop`）。K=4 时 FEC 让上线包数多 25%，
分子（实际丢的包）跟着涨，分母没涨。

$$14.0\% \approx 10.4\% \times 1.25$$

**后果**　一个**纯粹由分母造出来的假象**，而且方向恰好和要证明的相反。
这种数最容易被当成真结论——它有具体数字、有对照组、看起来还挺"反直觉"。

**做法**　分母改成 `packets_sent + fec_packets + retransmitted`。
改完之后各格注入率一律落在 10.4% 附近，与 FEC 开关无关。

---

### 问题 3 · 关 NACK 时把测量仪器一起关掉了

**现象**　消融表里"全关"和"只 FEC"两格的**真丢包数是 0**，
而同一格只渲染出 47 / 1000 帧。

**根因**　关 NACK 用的是接收端的 `--nack-window=0`，而它关掉的是 `NackTracker` **本身**——
`lost_exact` 和 `nack_pending` 都出自这个类。类不跑，两个数就恒为 0。

**后果**　那两格看起来"一个包都没丢"，而实际上丢了 10.5%。
**把温度计拔了当退烧。**

**做法**　改成在**发送端** `--retx-ms=0`：前向路径和"没有重传"完全一致
（请求发过去没人理），而接收端照样逐 seq 记账。
代价是这几格 `nack_sent` 不为 0，读表时别误认为重传开着。

> 一般化：**关一个机制**和**关这个机制的观测**是两件事。
> 消融实验里必须只关前者，否则关掉的那一格没有数据，而不是"数据是 0"。

---

### 问题 4 · sudo 凭据中途过期，g3/g4 全部跳过

**现象**　`g3`、`g4` 的每一格都打印一行 `!! netem 设置失败, 跳过这一格`，
脚本正常跑完、返回 0、汇总照常生成。

**根因**　时间线：

| 时刻 | 事件 |
|---|---|
| 14:43:51 | 脚本开头探针 `sudo -n tc qdisc ... netem delay 1ms` 通过，`HAVE_NETEM=1` |
| — | g0 + g1 + g2 共 36 次运行 |
| ~14:59 | sudo 凭据到期（`timestamp_timeout` 默认 **15 分钟**） |
| 15:04:30 | g3 第一格开始，`sudo -n` 需要密码 → 失败 |

**后果**　**唯一能给 M4.3 自适应水位和"17ms 重传预算"预测提供证据的两组，
一格都没跑成**，而脚本的退出码、汇总文件、进度输出全都正常。
20 分钟的等待换来一份看起来完整、实际上缺了两组的报告。

**做法**　三处：

1. 开头改用 `sudo -v`（交互式，允许输密码）申请一次，然后挂一个后台循环每 60 秒 `sudo -n true` 续期；
2. 探针改用**真正要用的那条语法** `delay 10ms 5ms`，不是 `delay 1ms`——
   探针和实际命令不一样的话，语法问题同样会在 20 分钟后才暴露；
3. 组的顺序改成 `g0 → g3 → g4 → g1 → g2 → g5 → g6`，**脆的先跑**。
   g0 仍然排最前，它是自检。

> 一般化：**长跑的实验台里，任何有生命周期的凭据都会在你不看的时候过期。**
> 而"跳过"和"跑完了但结果是空的"在退出码上完全一样。

---

### 问题 5 · 漏采了一个计数器，最重要的发现只能靠推断

**现象**　`loss5` 那格组好 1000 帧、解码只出 987 帧，中间少的 13 帧去哪了说不清。

**根因**　`RX_KEYS` 里漏了 `dropped`（组包器丢弃的整帧数）。
收尾日志里有这个字段，采数脚本没抄。

**后果**　只能靠排除法推断（`queue_dropped=0`、`jitter_dropped=1`，
所以剩下的只能是解码器自己没吐出来）。结论大概率对，但**是推出来的不是量出来的**。

**做法**　补进 `RX_KEYS`。

---

### 数据说了什么

一、**消融表成立，而且有一处独立的交叉验证**（10% 丢包，1000 帧，3 种子均值）：

| 配置 | 渲染 | 真丢包率 | NACK 救回 | FEC 救回 |
|---|---|---|---|---|
| 全关 | **47** | 10.5% | 0 | 0 |
| 只 FEC | 590 | 2.6% | 0 | 558 |
| 只 NACK | 998 | 0.02% | 817 | 0 |
| FEC + NACK | 995 | **0** | 261 | 558 |

「只 FEC」剩下 **261** 个包没救回来，「FEC+NACK」里 NACK 恰好救回 **261** 个。
两个数是从不同计数器独立算出来的，对得上说明口径没问题。

「全关」那格三个种子里**两个一帧都没渲染出来**。IDR 约 25~30 个分片，
$0.9^{30} \approx 4\%$，起播门等不到一个完整的关键帧就一直关着——
不是画质下降，是**根本起不来**。

二、**一帧丢掉会连累约半个 GOP**（`gop=30`）：

| 格 | jitter 丢帧 | 解码缺口 | 倍数 |
|---|---|---|---|
| loss3 | 0.7 | 8.7 | 12.4× |
| loss5 | 1.0 | 13 | 13× |
| loss10 | 0.3 | 1.3 | 4.3× |

这解释了丢包扫描为什么**不单调**（5% 的成绩比 10% 差）：低丢包时自适应水位
停在 `minDelayMs = 10` 下限，接不住一次重传往返；到 10% 丢包时水位涨到 36ms，
反而够了。

**`minDelayMs = 10` 是拍出来的，没有出处。** 它应该由重传预算推出来
（检测延迟一个帧周期 33ms + RTT），这是设计上的一个洞。

「约等于半个 GOP」目前只是**和数据一致**，不是测出来的。新增 G6（`loss=5` 扫
`gop` 10/30/60）去坐实：缺口跟着 gop 走才算测到，不跟着走这句话就不能写进报告。

三、**PLI 从头到尾一次都没触发。** 全部 60 格里 `pli_sent`、`nack_gaveup`、
`resyncs` 恒为 0；g5 三格（PLI 关 / 1s / 100ms）渲染数完全相同。

而这一轮实际观察到的故障（丢一帧 → 十几帧解不出来）**同时不满足两个触发条件**：
B 要队列 A 满，D 要 NACK 在关键帧分片上放弃。
PLI 有单测、有变异验证，但**端到端从未生效过**——报告里必须这么写。

---

### 这一轮的数不能用来说什么

**回环 RTT ≈ 0，NACK 的成绩是虚高的。** 重传有 4 次机会，30% 丢包下残留
$0.3^4 = 0.81\%$——所以「30% 丢包仍渲染 98.6%」量的是
**"重传免费时能有多好"**，不是"弱网下能有多好"。

给它标价的就是没跑成的 g4。在 g4 有数之前，这个百分比不进 README。

---

### 下一轮

1. `./scripts/m45_bench.sh g3 g4 g6`（约 10 分钟）——补上三组缺口；
2. 有数之后再决定 `minDelayMs` 该怎么定；
3. PLI 的端到端触发要么另造场景，要么在报告里如实写"未验证"。

---

## 2026-09-01 · M4.5 第二轮（补跑 g3 / g4 / g6）

**跑了什么**：`./scripts/m45_bench.sh g3 g4 g6`，14 格 × 3 种子。commit `2f7d31b`。

**收场**：sudo 保活生效，`netem 设置失败` 一次都没出现，14 格全跑完。
**但 g3 和 g4 的数据是假的。**

---

### 问题 6 · netem 挂上了却不起作用，两组数据全是假的

**现象**　g3 的三档抖动和无损伤那格**完全相同**：

| 格 | 水位 | p50 | p95 |
|---|---|---|---|
| clean_adapt | 10 | 13.0 | 46.0 |
| j10_5_adapt | 10 | 13.0 | 46.3 |
| j20_10_adapt | 10 | 13.0 | 46.3 |
| j30_15_adapt | 10 | 13.0 | 46.0 |

连 10ms 的**基础单向延迟**都没出现——那本该把 p50 从 13 推到 23。
g4 更直白：`rtt10 / rtt30 / rtt50` 三格的 `nack_recovered` 都是 **261**、
`fec_recovered` 都是 **557.7**，和完全没挂 netem 的 `g2/loss10` 一字不差。

**根因**　两层：

1. WSL2 的 `networkingMode=mirrored` 另起了一个 **`loopback0`** 设备
   （`ip -br link` 里看得到，MAC 是 Hyper-V 的 `00:15:5d:` 段），
   127.0.0.1 的流量走它，**不经过 `lo` 的 qdisc**。netem 挂在 `lo` 上等于挂在空气上。
2. 而脚本只检查了 `tc qdisc replace` 的**退出码**。tc 返回 0、qdisc 也确实挂上了——
   它汇报的是"我收到你的指令了"，不是"包被延迟了"。

**后果**　14 格全部跑完，没有一行报错，汇总齐整。
**两组数据看起来是有效的实验结果**，而它们只是把 `g2/loss10` 又跑了一遍。
要不是三档抖动的 p50 一模一样（13.0 / 13.0 / 13.0），这份数据会被直接写进报告。

**做法**　两条：

1. `netem_probe_dev` 逐个设备**实测**：挂 10ms 之后 `ping 127.0.0.1` 真的慢了才认，
   `lo` 不行就换 `loopback0`；一个都不行就明确跳过 g3/g4，
   而不是挂着一个不起作用的 netem 继续跑；
2. 每一格设完 netem 再量一次——凭据过期、设备被别的东西改掉，中途都可能失效，
   而失效之后 tc 照样返回 0。

> 一般化：**"命令成功了"和"命令起作用了"是两件事。**
> 凡是通过内核/驱动/外部工具改变环境的操作，退出码只能证明前者。
> 这和[问题 3](#问题-3--关-nack-时把测量仪器一起关掉了)是同一类错误——
> 查了个不相干的东西然后信了它。

---

### G6 · 「半个 GOP」既没被证实也没被推翻

| gop | jitter 丢帧 | 解码缺口 | 倍数 | 若为 gop/2 则应为 |
|---|---|---|---|---|
| 10 | 2.7 | 13.3 | **4.9×** | 5 |
| 30 | 1.0 | 13.0 | **13×** | 15 |
| 60 | 0.3 | 0.4 | 1.3× | 30 |

gop=10 和 gop=30 两档**几乎正好落在 gop/2 上**。gop=60 完全对不上，
但那一档三个种子加起来只发生了 **1 次**丢帧事件——样本量是 1。

**所以这条结论现在还不能写。** 要坐实，要么给 gop=60 加种子直到攒够十几次事件，
要么另造一个能**指定丢第几帧**的场景，直接数它连累了多少帧。
后者更快也更硬，但需要给注入器加一个"只丢指定 seq"的模式。

---

### 这一轮之后，M4 还缺什么

| 结论 | 状态 |
|---|---|
| 消融表（FEC / NACK 各值多少） | ✅ 第一轮已成立，含一处独立交叉验证 |
| 自适应水位在**干净链路**上省延迟 | ✅ p50 53.7 → 13.0，帧一个不少 |
| 自适应水位在**抖动链路**上的表现 | ❌ 未测——netem 没生效 |
| 「17ms 重传预算」预测 | ❌ 未测——同上 |
| 一帧丢失连累半个 GOP | ⚠️ 两档吻合，一档样本量为 1 |
| PLI 端到端 | ❌ 从未触发（89 格里 `pli_sent` 恒为 0） |

**回环 RTT ≈ 0 这条约束到现在还没被绕开。** 在它被绕开之前，
「30% 丢包仍渲染 98.6%」量的是"重传免费时能有多好"，不进 README。

---

## 2026-09-01 · M4.5 第三轮（`g3 g4`，设备探测已加）

**跑了什么**：`./scripts/m45_bench.sh g3 g4`。commit `386e4e6`。
探针打印 **`netem 生效设备: lo`**，每一格的生效检查也全部通过。

**收场**：14 格全跑完，**数据和第二轮一模一样**——
g3 三档抖动的 p95 都是 46~47、水位都卡在 10ms 下限，
g4 三档 RTT 的结果和不挂 netem 的 `g2/loss10` 没有区别。

---

### 问题 7 · 探针查的是 ICMP，被测的是 UDP

**现象**　这一轮和上一轮的区别在于：上一轮探针**没通过**（后来才知道是它压根没查对东西），
这一轮探针**通过了**——`netem delay 10ms` 挂上 `lo` 之后，`ping 127.0.0.1` 确实变慢了。
然而应用流量一点没变。

期望：`delay 30ms 15ms` 应当把 p95 推高约 44ms、把水位顶到约 28ms。
实测：p95 46 → 47，水位 10 → 10。

**根因**　**ICMP 和 UDP 走的不是同一条路。**
WSL2 的 mirrored 模式为了让 Windows↔Linux 的 localhost 互通，
把回环的 TCP/UDP 接管走了另一条路径；而 `ping` 用的 ICMP 仍然走标准的 `lo`。
netem 挂在 `lo` 上，于是**只延迟了 ping**。

**后果**　比上一轮更糟：上一轮探针失败还留了个"设置失败"的痕迹，
这一轮探针**报告成功**，每一格的复查也报告成功，
7 分钟跑完 14 格，输出里没有任何异常。
**一个自称已经验证过的假阳性。**

**做法**　探针换成 `scripts/udp_rtt.py`：绑一个 UDP socket 发给自己、量往返中位数。
和被测流量**同协议、同地址族**。ICMP 仍然量，但只在报错时用来说明
"ping 慢了而 UDP 没慢"，不作判据。

另外单独加了 `scripts/netem_check.sh`：20 秒内把每个回环设备的 UDP/ICMP 往返
都量一遍并给结论。**跑 7 分钟的实验之前先花 20 秒问清楚它到底生不生效。**

---

### 三次是同一个错误

| 轮次 | 我查了什么 | 该查什么 |
|---|---|---|
| 一 · [问题 3](#问题-3--关-nack-时把测量仪器一起关掉了) | `--nack-window=0` 关掉了 NACK | 它同时关掉了统计丢包的那个类 |
| 二 · [问题 6](#问题-6--netem-挂上了却不起作用两组数据全是假的) | `tc` 的退出码 | 包有没有真的被延迟 |
| 三 · 问题 7 | ICMP 变慢了 | **UDP** 有没有变慢 |

每一次查的东西都**紧挨着**要验的那件事，近到看起来就是它。
而三次的后果完全一样：**脚本一切正常，数据全是假的。**

能把它们一次抓住的判据只有一条：**验证必须落在被测对象本身上**——
同一个类、同一条链路、同一种协议。任何"相关的东西正常"都不算。

---

---

## 2026-09-01 · M4.5 第四轮（`g3 g4`，netem 第一次真的生效）

**跑了什么**：`./scripts/m45_bench.sh g3 g4`。commit `9230c0b`。

探针这次给出了完整的诊断：

```
(lo: UDP 往返 0.147ms — 没生效; 注意 ICMP 是 20.743ms,
     说明 ping 走的路和 UDP 不是同一条)
netem 生效设备: loopback0
```

**问题 7 的假设被直接证实**：同一条 `netem delay 10ms` 挂在 `lo` 上，
ICMP 慢了 20.7ms 而 UDP 只有 0.147ms。前三轮的 g3/g4 数据全部作废。

---

### 问题 8 · 生效判据卡太满，误杀三格

**现象**　`j20_10_adapt`、`j30_15_fixed`、`j30_15_adapt` 三格被自己的检查跳过：

```
!! 期望 UDP 往返 >= 20ms, 实测 15.294ms
!! 期望 UDP 往返 >= 30ms, 实测 25.003ms
```

**根因**　`netem delay 20ms 10ms` 是 **[10, 30] 上的均匀分布**，
7 个样本的中位数散得很开；而判据要求 `>= 20`（配置值本身），
**大约一半的概率会误判**。判据设计错了，netem 是好的。

**后果**　g3 里最关键的两档抖动没跑成——恰恰是水位应该明显抬起来的那两档。

**做法**　样本数 7 → 21；阈值改成配置值的 **50%**。
判据只需要区分"生效"和"完全没生效"，而这两者相差 **100 倍**
（没生效时回环 UDP 往返是 0.15ms 量级），没有任何理由卡满。

---

### 发现 A · 估计器看不见证明它错了的那些帧（严重）

`g4/rtt50` 三个种子分成两个极端：

| 运行 | 渲染 | jitter 丢帧 | 水位 | **水位峰值** | nack 放弃 | PLI |
|---|---|---|---|---|---|---|
| rtt50_s1 | **632** | 150 | 10 | **50** | 3 | **1** |
| rtt50_s2 | **623** | 131 | 10 | **50** | 0 | 0 |
| rtt50_s3 | 980 | 6 | 87 | 87 | 8 | 0 |

`jitter_peak = 50` 是关键证据：s1/s2 的水位**从初值 50 一路衰减到下限 10，
从来没有涨过一次**——而同期有 130~150 帧因为太晚被丢掉。

**根因**　`JitterBuffer::push()` 的顺序：

```
1. extendFrameId
2. 起播门
3. framesTooLate 检查    ← 迟到的帧在这里被丢掉
4. 查重
5. computePlayAt         ← 估计器的样本在这里才取
```

一帧迟到就在第 3 步被扔了，**永远不会成为第 5 步的样本**。
于是估计器只看得到准时到达的帧——那些按定义就是快的。
p95 算出来很小 → 水位收窄 → 更多帧迟到 → 采样更加只剩快的。

**正反馈，而且是幸存者偏差长在控制器自己的输入上。**

系统因此是**双稳的**：s3 的早期样本恰好把水位顶到 87，锁在好的平衡点；
s1/s2 没顶上去，掉进坏的。同样的配置、同样的链路，结果差 1.6 倍。

> 这是 M4.3 设计时完全没想到的一层。当时的推理是"水位跟着 offset 的分位数走"，
> 而没有问**哪些帧有资格贡献 offset**。

**怎么修是设计决定，不在这份记录里定。** 方向是让迟到的帧也留下样本
（把取样挪到 too-late 检查之前，或者为被拒的帧单独记一笔）——
那样估计器才看得到"这一帧晚了 84ms"这件事。

---

### 发现 B · 「17ms 重传预算」这个预测是错的

| | nack_sent | nack_recovered |
|---|---|---|
| rtt10 | 217 | 262 |
| rtt30 | 242 | 261 |
| rtt50 | 439 | 264 |

设计时的预测是：抖动 50ms − 检测延迟 33ms = 17ms 预算，
所以 RTT 超过约 17ms 之后 **NACK 会迅速失效、FEC 接管**。

**实测 RTT 50ms 时 NACK 照样救回 264 个包，一个没少。**
请求数翻倍（217 → 439）是重发，恢复量没有掉。

真实情况比原预测更准确也更有用：

> **NACK 一直在工作。救回来的包是被 jitter buffer 当成迟到帧扔掉了。**

失效的不是重传，是下游把重传的成果丢了——而那正是发现 A 那个盲区。
两条连在一起：迟到的帧既不被播放，也不被计入水位估计，
于是系统既没拿到那一帧，也没学到"我该多等一会儿"。

---

### 发现 C · PLI 第一次真的发出去了

`rtt50_s1`：`nack_gaveup=3`，`pli_sent=1`。触发源 D 生效。

样本只有 1 次，说明不了限流做得对不对，但至少这条链路**端到端跑通过一次**了——
前三轮 89 格里它一次都没响过。

---

### G3 已有的部分

| | netem | 水位 | p95 |
|---|---|---|---|
| clean_fixed | — | 50 | 54 |
| clean_adapt | — | 10 | 46 |
| j10_5_fixed | 10±5 | 50 | 61~63 |
| j10_5_adapt | 10±5 | 10 | 56~58 |
| j20_10_fixed | 20±10 | 50 | 70~73 |
| j20_10_adapt | 20±10 | — | **被误杀** |
| j30_15_* | 30±15 | — | **被误杀** |

固定水位那一列随抖动线性变差（54 → 62 → 71），自适应那一列一直更低。
但**自适应的三格里有两格没跑**，而且按发现 A，正是抖动大的时候
估计器最可能瞎掉——所以现在还不能说"自适应在抖动链路上更好"。

---

### 下一轮

1. 重跑 `g3 g4`（判据已放宽）——补上被误杀的三格；
2. 发现 A 是设计问题，要先定怎么改，改完再重跑 g4；
3. `minDelayMs = 10` 这个下限现在有第二条罪证了：
   它既接不住重传往返（第一轮），又是坏平衡点的落脚处（这一轮）。

---

## 2026-09-01 · M4.5 第五轮（`g2 g3 g4`，M4.6 修复之后）

**跑了什么**：`./scripts/m45_bench.sh g2 g3 g4`。commit `927fbe8`。
netem 生效设备 `loopback0`，全部格子都跑成了。

---

### 结果 1 · 双稳消失了（M4.6 的主要目的达成）

`g4/rtt50`，三个种子：

| | 修复前 | 修复后 |
|---|---|---|
| 渲染 | 632 / 623 / 980 | **998 / 997 / 998** |
| jitter 丢帧 | 150 / 131 / 6 | **0.3** |
| 水位 | 10 / 10 / 87 | 107 / 120 / 120 |
| 水位峰值 | 50 / 50 / 87 | —— 不再"只降不升" |

`sampled_dropped` 全程非 0，说明迟到的帧确实进了估计器；
`floor_src` 在有 RTT 的格子里一律是 `budget`。两条链路都通了。

---

### 结果 2 · 我的预测错了 36%，而错因是两个改动互相干扰

上一轮我写下的预测是：**rtt50 的 p95 从 75 涨到约 120**。
**实测 162.7。**

推导是 `水位 ≈ 帧周期 + RTT = 33 + 50 = 83`，加上管线开销约 46，得 129。
实测各项：

| 格 | frame_int | rtt | floor | 水位 | p95 |
|---|---|---|---|---|---|
| rtt10 | 33.7 | 12 | 45.7 | 46.0 | 83.0 |
| rtt30 | 33.7 | 32 | 65.7 | 69.0 | 116.0 |
| rtt50 | **55.7** | 53 | 108.7 | 115.7 | 162.7 |

**帧周期在 rtt50 那格被量成了 55.7ms，而真值是 33ms。**

根因是我这两个改动互相干扰：

- 采样点前移之后，**每一帧**都会喂进估计器，包括那些靠重传补齐、
  因而在好几帧之后才到达的；
- `updateFrameInterval` 是在 `observe` 里按到达顺序取相邻时间戳的差，
  于是乱序的到达序列产生一串偏大的 |delta|；
- RTT 越大，需要重传的帧回来得越晚、乱序越重。rtt10/rtt30 只轻微偏（33.7），
  rtt50 就顶到了 55.7。

我原先的设计注释写的是"取中位数，乱序歪不了它"。
**那句话在乱序是少数时成立，而修复之后乱序成了多数**——
10% 丢包下一帧 8 个分片，`1-0.9^8 = 57%` 的帧要等重传。

代价：`floor` 被抬高约 23ms，等于每帧白付 23ms 延迟。

> 一般化：**改一处会改变另一处的输入分布**。
> 这里帧周期估计器的输入本来是"按序到达的时间戳流"，
> 采样点前移把它变成了"按到达顺序、大量乱序的流"，而估计器的假设没跟着变。

---

### 结果 3 · 下限没能治好低丢包那个坑

`g2` 的低丢包三格，和第一轮**逐帧相同**：

| | 第一轮 decoded | 第五轮 decoded | 水位 | jitter 丢帧 |
|---|---|---|---|---|
| loss1 | 1000 | 1000 | 10 → 34 | 0 |
| loss3 | 991.3 | **991.3** | 10 → 34 | 0.7 |
| loss5 | 987.0 | **987.0** | 10 → 34 | 1.0 |

水位从 10 抬到 34，而结果一帧不差。

根因：回环 RTT 只有 1ms，于是 `floor = 帧周期 33 + RTT 1 = 34` ——
**恰好等于一次典型重传，一点余量都没有**。而真正被丢掉的那一帧，
是需要**第二次**请求才回来的那种。按典型值定的预算接不住它。

结论：**这条预算的形式对，值太紧。** 它管住了 g4 的双稳（那里 RTT 大，
预算也大），但管不住"偶尔一帧要两轮重传"。

而低丢包这个坑的真正代价还是那个放大倍数：**丢 1 帧 → 13 帧解不出来**
（`gop=30`）。用缓冲把它买回来，要按**最坏**的重传定水位而不是典型值 ——
那太贵了。这件事本来就该归 PLI，而 PLI 在这一轮**仍然一次都没触发**。

---

### 结果 4 · 自适应水位：p50 赢很多，p95 几乎不赢

`g3`，三个种子均值：

| 抖动 | 固定 p50 | 自适应 p50 | 固定 p95 | 自适应 p95 | 自适应水位 |
|---|---|---|---|---|---|
| 无 | 54.0 | **13.0** | 54.0 | **46.3** | 10 |
| 10±5 | 61.3 | **24.0** | 62.3 | **56.3** | 10 |
| 20±10 | 67.7 | **35.0** | 70.0 | **65.0** | 10 |
| 30±15 | 72.7 | **49.0** | 75.0 | 76.0 ← **反超** | 15 |

两侧都渲染 1000 帧，一帧不丢。

**中位延迟省 24~41ms，而 p95 只省 0~8ms，抖动最大那档还小输 1ms。**

原因是缓冲同时是个**方差抑制器**：水位 50 时每帧都要等，交付节奏被拉齐
（p95 − p50 只有 1~3ms）；水位 10 时帧一到期就放，交付节奏跟着到达抖动走
（p95 − p50 达到 32ms，正好一个帧周期）。
**自适应买的是中位延迟，卖的是尾延迟的稳定性。**

### 附带发现 · 分片本身就吃掉了大部分链路抖动

自适应水位在 30±15ms 的链路上只有 15ms —— 远小于链路抖动。
不是估计器瞎了，是**一帧要等它最慢的那一片**：

一帧 8 个分片，每片的传输时间独立地落在 [15, 45]，
帧的到达时间是这 8 个的**最大值**。最大值的分布窄得多：

$$p_{95} - p_{5} = 30 \times (0.95^{1/8} - 0.05^{1/8}) \approx 9.2\ \mathrm{ms}$$

实测 15ms（多出来的是编码耗时的方差）。同一个量级。

**所以 jitter buffer 看到的抖动比链路小得多**，
而这正是自适应的优势不随链路抖动增长的原因。分片替它挡掉了。

---

### 顺带 · 一处配置不一致

`DelayEstimatorConfig::minDelayMs` 的结构体默认值改成了 5，
而 `app/receiver/main.cpp` 里 `--jitter-min-ms` 的默认仍是 10 —— 后者赢。
`g3` 那几格 `jitter_floor` 显示 10 就是这么来的（那里没有 RTT，预算不生效）。
不影响结论，但两个默认值该对齐。

---

### 这一轮之后的账

| 结论 | 状态 |
|---|---|
| 消融表 | ✅ 第一轮成立 |
| 估计器盲区 → 双稳 | ✅ 已修，已验证 |
| 自适应省中位延迟 | ✅ 24~41ms，代价是尾延迟方差变大 |
| 自适应在大抖动下更好 | ❌ **不成立**，30±15 时 p95 小输 |
| 「17ms 重传预算」预测 | ❌ 第四轮已推翻 |
| 重传预算当下限 | ◐ 治住了双稳，治不住低丢包的单帧丢失（值太紧） |
| 帧周期估计 | ❌ 被乱序污染，rtt50 下偏高 69% |
| 一帧丢失连累半个 GOP | ⚠️ 仍是两档吻合、一档样本量为 1 |
| PLI 端到端 | ❌ 五轮里只在第四轮响过 1 次 |

---

## 2026-09-01 · M4.5 第六轮（`g4`，p25 修复之后）

**跑了什么**：`./scripts/m45_bench.sh g4`。commit `7d625aa`。

### 预测与实测

上一轮写下的预测，以及这一轮的实测（三个种子均值）：

| | 第五轮 | **预测** | **实测** |
|---|---|---|---|
| `frame_int` | 55.7 | ≈ 33 | **33.0** |
| `jitter_floor` | 108.7 | ≈ 86 | **84.0** |
| p95 | 162.7 | ≈ 140 | **134.7** |
| 渲染 | 997.7 | ≈ 998 | 992.7 |

**前三项都落在预测里。** 帧周期在 rtt10 / rtt30 / rtt50 三格全部量成 33.0
（第五轮 rtt50 是 55.7），乱序污染消失；下限 84 = 33 + 51，正好是
帧周期加实测 RTT。

### 代价：省 28ms 延迟，多丢 5 帧

| | 第五轮 | 第六轮 |
|---|---|---|
| 渲染 | 997.7 | **992.7**（996 / 997 / 985） |
| jitter 丢帧 | 0.3 | **1.3** |
| `nack_gaveup` | 0 | **3.7** |
| `pli_sent` | 0 | **0.3** |

水位从 108.7 收到 84 之后，每 1000 帧多丢约 5 帧。**这是一次交换，不是白捡：**
第五轮那个偏高的下限是帧周期估计错了的副产品，它顺带多买了 24ms 的余量，
而那 24ms 确实接住了几帧。修对之后余量没了，那几帧就丢了。

顺带：`nack_gaveup` 从 0 变成 3.7，PLI 因此在一个种子上响了一次。
缓冲一紧，NACK 的重试次数就更容易用完 —— 这条链路第一次在**非极端**配置下被走通。

---

### 一个我解释不了的数

`g4/rtt10` 的 p95 从 **83.0 掉到 54.0**，而水位只从 46 变成 45。

| | 第五轮 | 第六轮 |
|---|---|---|
| 水位 | 46.0 | 45.0 |
| `frame_int` | 33.7 | 33.0 |
| p50 | 55.0 | 53.0 |
| **p95** | **83.0** | **54.0** |
| p95 − p50 | 28.0 | 1.0 |

p50 只动了 2ms，p95 动了 29ms —— **变的是尾巴，不是主体**。
而 p25 那个改动在 rtt10 这一格只把下限挪了 1.7ms，解释不了 29ms。

两轮各自内部都很稳（第五轮三个种子全是 83，第六轮全是 54），
所以不是随机噪声，是两轮之间有系统性差异。

目前唯一的候选：**机器状态**。第五轮跑的是 `g2 g3 g4` 共 60 格、约 20 分钟，
g4 排在 g3 的 24 格之后；第六轮只跑 g4 三格、2 分钟，机器是凉的。
p95 里含解码耗时，而**尾延迟对 CPU 竞争敏感，中位数不敏感** ——
这和 p50 只动 2ms、p95 动 29ms 的形状吻合。

**但这只是候选，没有证据。** 要证实就得把 g4 单独跑一次、再接在一长串格子后面跑一次，
比两次的 p95。

> 如果成立，它是一条方法论的账：**长跑的实验台里，后跑的格子和先跑的格子
> 不在同一个机器状态下**，而尾延迟正好是最受这件事影响的量。
> 那样的话，跨轮次比较 p95 必须让格子处在同一个位置上，
> 否则比的是"跑到第几分钟"而不是配置。

---

### M4 实验部分的收口

| 结论 | 状态 |
|---|---|
| 消融表（FEC / NACK 各值多少） | ✅ |
| 估计器盲区 → 双稳 | ✅ 已修、已验证 |
| 帧周期被乱序污染 | ✅ 已修、预测与实测吻合 |
| 自适应省中位延迟 | ✅ 24~41ms，代价是尾延迟方差变大 |
| 自适应在大抖动下更好 | ❌ 不成立，30±15 时 p95 小输 1ms |
| 「17ms 重传预算」预测 | ❌ 已推翻，换成了更准确的说法 |
| 重传预算当下限 | ◐ 治住双稳；治不住低丢包的单帧丢失（见 [[D25]]） |
| 一帧丢失连累半个 GOP | ⚠️ 两档吻合、一档样本量为 1 |
| PLI 端到端 | ◐ 六轮里响过 2 次（第四、第六轮），都在 rtt50 |
| `g4/rtt10` 的 p95 跳变 | ❓ 无解释，候选是机器状态 |

---

## 2026-09-01 · M4.5 第七、八轮（复现 + 分辨实验）

**第七轮**：`g4` 单跑，与第六轮**逐格相同**（rtt10 p95 54，rtt30 84，rtt50 135）。
确认 g4 单跑是可复现的。但两轮的条件完全一样（机器凉、g4 第一个跑），
所以它证明不了机器状态那件事。

**第八轮**：`g3 g4` —— g4 排在 g3 的 24 格之后，和第五轮的位置相同。
数据在 `bench/20260901_230840/`（终端输出丢了，磁盘上是全的，33 行）。

### 「机器状态」这个假设被推翻了

| 轮次 | g4 的位置 | 代码 | rtt10 p95 |
|---|---|---|---|
| 五 | g3 之后 | `927fbe8` | **83** |
| 六 / 七 | 单跑 | `7d625aa` | 54 |
| **八** | **g3 之后** | `7d625aa` | **54** |

**位置相同、结果不同；位置不同、结果相同。** 变量是代码，不是机器状态。

所以第六轮记的那条"跨轮次比较 p95 必须让格子处在同一个位置上"**不成立**，
g2 / g3 在长批次里测出来的 p95 不必打问号。

### 但"哪一处代码、什么机制"仍然没定论

第五轮到第八轮之间只有两处改动：`minDelayMs` 10→5，以及帧周期从中位数改成 p25。
rtt10 那一格的下限由预算给出（44ms），`minDelayMs` 不参与，所以嫌疑在 p25。

推测的机制：中位数版本的 `frame_int` 在**运行过程中**会随乱序上下漂，
带着 `floor` 和水位一起漂，而 p95 把这些漂移都收进了尾巴里；
p25 之后 `floor` 被钉死，尾巴就塌了。第五轮 rtt10 的 `frame_int` 收尾值是 33.7，
只比 33 高一点——但**收尾值看不出过程**。

第八轮的每秒轨迹支持这个方向（rtt10 全程 45~46ms，35 秒里一次没动），
但那是修好之后的样子，**不能反过来证明修之前是漂的**。

> 能定论的证据是第五轮的每秒轨迹，而**那些日志被我删了** ——
> 前几轮的提交命令里带了 `rm -rf bench`，而 `bench/` 本来就在 `.gitignore` 里，
> 那个清理既多余又是破坏性的。已经停止。

**要定论只需两分钟**：把 p25 改回中位数，单跑一次 `g4`，看 rtt10 的 p95 是不是回到 83。

---

### G3 定稿：p50 的差异是信号，p95 的差异是噪声

第八轮，每格三个种子分别列出：

| 抖动 | 固定 p50 | 自适应 p50 | 固定 p95 | 自适应 p95 |
|---|---|---|---|---|
| 无 | 53 / 53 / 53 | **8 / 8 / 8** | 53 / 53 / 53 | 46 / 46 / 45 |
| 10±5 | 60 / 59 / 60 | **19 / 19 / 19** | 61 / 59 / 60 | 57 / 57 / 56 |
| 20±10 | 67 / 66 / 68 | **34 / 34 / 34** | 67 / 71 / 71 | 64 / 64 / 64 |
| 30±15 | 72 / 77 / 77 | **49 / 49 / 48** | 73 / 82 / 82 | 71 / 78 / 76 |

八格全部渲染 1000 帧，一帧不丢。

- **p50：省 25~45ms，种子间散布 ≤ 1ms。** 信号远大于噪声。
- **p95：省 3~7ms，而单格内种子间散布最大到 9ms**（`j30_15_fixed` 是 73/82/82）。
  **信号小于噪声 —— p95 上测不出差异。**

**这同时更正第五轮的一条记录**：那一轮写"30±15 时自适应的 p95 小输 1ms（反超）"，
当成了一个发现。看到种子间散布之后可以确定**那 1ms 也是噪声**，
第八轮同一格反而是自适应领先 4ms。**一个格子只看均值、不看散布，就会把噪声读成结论。**

> 所以报告里这一项只能这么写：**自适应把中位延迟砍掉一半到六分之一，
> 而尾延迟与固定水位没有可测的差异。**

### 附带：水位下限改成 5 之后

自适应水位从 10/10/15 变成 5/9/13~15 —— 干净链路和小抖动那两档落到了新的绝对下限 5，
而 20±10 / 30±15 两档是估计器自己算出来的（9、13~15），没碰下限。
这说明 `minDelayMs` 在有抖动的链路上本来就不参与，改它只影响近乎无抖动的场景。

---

### 待决 · 抖动链路怎么造（只在 `loopback0` 也不行时才需要决定）

脚本现在会自己试 `lo` 和 `loopback0`，试成了就没有这个问题。
两个都不行的话，摆在面前的是三条路。

**要用它换的是什么**：M4 里有两条结论，只有真延迟能验：

- **自适应水位在抖动链路上到底有没有用。** 干净链路那半已经测到了
  （p50 53.7 → 13.0，帧一个不少），但那是"网络比假设的好"——容易的一半。
  难的一半是"网络变差时水位跟得上，帧不过期"，而这才是这个功能存在的理由。
- **「17ms 重传预算」这个预测。** 设计时算的是：抖动 50ms − 检测延迟 33ms（一个帧周期）
  = 17ms 留给 RTT，所以 RTT 超过约 17ms 之后 NACK 应当迅速失效、FEC 接管。
  没有它，「30% 丢包仍渲染 98.6%」量的只是"重传免费时能有多好"。

| | 做法 | 代价 | 风险 |
|---|---|---|---|
| **甲** | **veth + netns**：造一对虚拟网卡，两端各放进一个网络命名空间，netem 挂在虚拟网卡上，收发两端分别 `ip netns exec` 起 | 要写一个建/拆环境的脚本；每次运行要套一层 netns；建环境要 root | 低。veth 是**真的**网络设备，netem 在上面一定生效；同一个内核 = 同一个 `steady_clock`，延迟数照样可比 |
| **乙** | `.wslconfig` 的 `networkingMode` 改回 NAT，`wsl --shutdown` 重启 | 一行配置 + 一次重启 | 中。mirrored 模式是为了 Windows↔WSL 的 localhost 互通，改回去可能影响别的开发流程；而且**不保证** NAT 模式下 `lo` 的 netem 就一定生效，改完还得再测一次 |
| **丙** | 不测，报告里把那两条如实写成"未验证" | 0 | 简历上这一项从"实现并验证了自适应抖动缓冲"降级成"实现了，干净链路上省了 40ms，弱网下未验证" |

**注**：跨机器不是第四条路。`Clock.h` 写死了 `steady_clock` 跨机器不可比，
而端到端延迟就是 `nowMs - captureMs`——两台机器的时钟原点毫不相干，
跨机器能比的只有丢包率、恢复率和带宽，延迟数全是垃圾。
**要延迟就必须单机**，这正是 netem 不是"退而求其次"而是唯一路径的原因。
