build/
*.o
*.h264
*.yuv
*.mp4
*.log
.vscode/
.cache/
.claude

# M4.5 实验台的输出: 日志很大, 结论进 docs/ 而不是进仓库。
# 前面的斜杠是必须的 —— 写成 `bench/` 会匹配**任意层级**的 bench 目录,
# 把存档用的 docs/bench/ 一起吞掉, 而 `git add -A` 对此不报任何错:
# 文件就是安静地没进去。
/bench/
